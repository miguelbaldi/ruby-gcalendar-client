Para utilizar o cliente google calendar, basta executar:
      gem sources -a http://gems.github.com
      sudo gem install miguelbaldi-gcalcliapp


Depois de instalar, basta rodar o programa desta forma:
        ruby gcalcliapp
Desta forma ele deve mostra a mensagem de help
